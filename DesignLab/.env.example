# API mode: mock | real
VITE_API_MODE=mock

# Backend base URL (used only in real mode)
VITE_API_BASE_URL=http://localhost:8080

# WebSocket URL (used only in real mode)
VITE_WS_URL=ws://localhost:8080/ws
